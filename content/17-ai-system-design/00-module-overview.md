# Module 17 — AI System Design

## Why this module comes seventeenth

Modules 7–16 built, shipped, secured, and measured the AI product: the model decision (M18), the app (M19), the knowledge (M20), the loop (M21), the automation (M22), the backend (M23), the cloud (M24), the pipeline (M25), the security (M26), and the observability (M27). Every one of them is **designed in a room** — and this module is that room. The L346 standard measures; this module is **the design that's measured**: the protocol (L347), the chat system (L348), the RAG platform (L349), the support and the sales (L350–351), the recruiting (L352), the document processing (L353), the coding assistant (L354), the e-commerce (L355), the automation platform (L356), the multi-tenant SaaS (L357), and the high-scale system (L358).

The distinction this module is built on: a **junior** answers with the pieces. A **solutions architect** runs the protocol (L347) — clarify → estimate → design → trade-offs — and defends the design (L358).

## Module map

- **M28 · AI System Design (L347–358)** — the round that decides the architect.
  The protocol (L347), the chat system (L348), the RAG platform (L349), the customer support (L350), the sales assistant (L351), the recruiting platform (L352), the document processing (L353), the coding assistant (L354), the e-commerce (L355), the automation platform (L356), the multi-tenant SaaS (L357), and the high-scale system (L358).

## How to study each lesson

1. **Run the protocol on every prompt (L347).** The four phases (L347): clarify → estimate → design → trade-offs — with the AI-specific questions per phase (L347). Every lesson in this module is the protocol (L347), run on a prompt (L348–358).
2. **Learn the repeatable shapes.** The chat (L348), the RAG (L349), and the agent (L200) — the shapes (L282) the designs assemble (L358).
3. **Apply the earlier modules.** The L260 backend (L260) is the design's floor plan; the L287 cloud (L287) is the deployment; the L307 pipeline (L307) ships it; the L325 security (L325) guards it; the L346 observability (L346) measures it. This module is the earlier modules, designed (L358).
4. **Build the capstone at the end (L358).** The final lesson runs the protocol (L347) on the high-scale AI system (L358) — millions of requests: the caching, the batching, and the cost of the scale (L358). Draw it, defend it, and M28 is claimed.

## Prerequisites

Module 12 (L233–260) — the backend floor plan (L260). Module 13 (L261–287) — the cloud (L287). Module 9 (L174–197) — the RAG (L197). Module 10 (L198–216) — the agents (L216). Module 14 (L288–307) — the pipeline (L307). Module 15 (L308–327) — the security (L325). Module 16 (L328–346) — the observability (L346). The L102 system-design spine (L102) — the web's protocol (L102).

## Next

→ [Lesson 347 — System Design Protocol for AI (L102 spine applied)](./347-ai-system-design-protocol.md)
