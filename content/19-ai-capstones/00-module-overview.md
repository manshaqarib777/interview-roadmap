# Module 19 — Capstone Projects

## Why this module comes nineteenth

Modules 7–18 built the AI architect's knowledge: the model decision (M18), the app (M19), the knowledge (M20), the loop (M21), the automation (M22), the backend (M23), the cloud (M24), the pipeline (M25), the security (M26), the observability (M27), the design (M28), and the enterprise (M29). Every one of them is **proven by a project** — and this module is the proof. The L380 toolkit is the bag; this module is **the projects that fill the portfolio**: the RAG SaaS (L381), the agent with the tools and the human approval (L382), the automation platform (L383), the multi-tenant AI SaaS (L384), the enterprise assistant (L385), and the complete architecture case study (L386).

The distinction this module is built on: a **specialist** has read. A **solutions architect** has built (L381–386) — the six projects (L386) are the portfolio (L103) that proves the architect.

## Module map

- **M30 · Capstone Projects (L381–386)** — the proof under the product.
  The RAG SaaS (L381), the agent with the tools and the human approval (L382), the automation platform (L383), the multi-tenant AI SaaS (L384), the enterprise assistant (L385), and the complete architecture case study (L386).

## How to study each lesson

1. **Build each project (L381–385).** The lessons (L381–385) are the project specs (L381): the requirements (L359), the architecture (L358), the build (L381) — the schema to the evaluation (L381).
2. **Learn the capstone shapes.** The RAG SaaS (L381) — the L349 platform (L349) shipped; the agent (L382) — the L216 loop (L216) with the L324 approvals (L324); the SaaS (L384) — the L357 capstone (L357) built.
3. **Apply the earlier modules.** The L358 design (L358) is the blueprint; the L287 cloud (L287) is the deployment; the L325 security (L325) is the guard; the L346 observability (L346) is the measure; and the L380 toolkit (L380) is the documentation (L386). This module is the earlier modules, built (L386).
4. **Build the capstone of the capstones (L386).** The final lesson assembles the complete architecture case study (L386) — the end-to-end design (L386), documented (L380): the requirements (L359), the ADRs (L361), the cost models (L368), and the diagrams (L380). Document it, defend it, and M30 — the final milestone — is claimed (L386).

## Prerequisites

Module 17 (L347–358) — the designs (L358). Module 13 (L261–287) — the cloud (L287). Module 14 (L288–307) — the pipeline (L307). Module 15 (L308–327) — the security (L325). Module 16 (L328–346) — the observability (L346). Module 18 (L359–380) — the enterprise (L380). The original roadmap's Portfolio Projects (L103) — the portfolio (L103) the capstones fill (L386).

## Next

→ [Lesson 381 — Project 1: Production RAG SaaS](./381-rag-saas.md)
